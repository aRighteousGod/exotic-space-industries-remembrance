interface:
  display_name: "Meshy API"
  short_description: "Use Meshy.ai API workflows safely"
  default_prompt: "Use $meshy-api to generate, inspect, look up animation library actions, or download Meshy.ai assets with environment-based credentials and safe local outputs. For ESIR/Factorio follow-up renders, hand downloaded GLBs to $meshy-blender-spritesheet with auto-ortho alpha margins and brightness controls before export. For web-app/manual assets, prefer documented task downloads or the official DCC Bridge; do not rely on undocumented personal-library routes."
