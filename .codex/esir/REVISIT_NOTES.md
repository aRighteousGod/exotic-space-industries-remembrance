# ESIR Revisit Notes

Use this file for short, durable follow-up notes when a patch intentionally leaves implementation debt, upgrade hooks, partial migrations, or known upgrade work behind.

Keep entries compact and practical:

- `YYYY-MM-DD | area | status | summary`
  - `Files:` absolute or repo-relative paths
  - `Next safe move:` the smallest sensible follow-up

Close or remove entries in the same patch that resolves them.

## Open

- `2026-04-20 | neutron-collector | open | hidden-proxy circuit telemetry is re-enabled after the collector proxy wiring and refresh paths were repaired, but the repo still lacks a dedicated neutron collector QC save/helper, so future regressions still depend on live play smoke or a locally supplied collector-bearing save.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/neutron-collector.lua`, `exotic-space-industries-remembrance/prototypes/quantum-age/neutron-collector.lua`, `.codex/esir/save-catalog.json`
  - `Next safe move:` add a tiny neutron collector runtime smoke save or helper that places one collector beside a live source and asserts the custom signals after a rebuild, then wire that save into `qc-runtime`.

- `2026-04-19 | orbital-logistics | open | cohort service now skips steady-state transponder claim rebuilds, shares one registry-scoped lease count pass across job build plus reconcile, builds each dispatch-surface registry from a dedicated transponder-unit index, refreshes platform-state transponders through a force+platform index instead of rescanning every transponder in the force on platform churn, rebuilds force claims from a dedicated force transponder index with narrow conflict dirtying, and lazily invalidates uplink silo dropdowns per surface so threshold edits skip unrelated binding-control repaints.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/orbital-logistics.lua`
  - `Next safe move:` benchmark the remaining dispatch render and launch-adjacent uplink lanes on the orbital QC helper, then decide whether the next safe trim is a staged uplink page/render cache or a cadence-spread change for cohort background work.

- `2026-04-18 | beacon-overload | open | steady-state enabled checks now ride the cached storage flag and internal recount paths reuse the same module state, so the next safe refinement is to benchmark whether `queued_units` and `queued_chunk_keys` are worth collapsing into the shared scheduler's unique-queue helpers.` 
  - `Files:` `exotic-space-industries-remembrance/scripts/control/beacon-overload.lua`, `exotic-space-industries-remembrance/scripts/control/global.lua`
  - `Next safe move:` profile one large rebuild, and only if queue bookkeeping still shows up, migrate the machine/chunk dedupe flow to `queue_push_unique`-style shared helpers without changing current rebuild order.

- `2026-04-18 | research-hitch-qc | open | the dedicated research-hitch benchmark stayed above its prior median after the latest hot-path trims, but the touched patch mostly affected normal tech-scaling cache reads and scripted-burst bookkeeping, not the helper's steady queued-research workload. The helper now records pre/post completion runtime snapshots through the QC remote bridge, direct headless validation fixed stale helper assumptions around `table.deepcopy`, `write_file`, and `research_queue_enabled`, and the save catalog now lets repo-side `runtime-benchmark` restage the helper automatically.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/tech-scaling.lua`, `exotic-space-industries-remembrance/control.lua`, `exotic-space-industries-remembrance/scripts/control/teslas-legacy.lua`, `exotic-space-industries-remembrance/scripts/control/em-trains/charger.lua`, `.codex/skills/esir-dev/assets/zzz-research-hitch-qc_0.0.1/control.lua`
  - `Next safe move:` inspect the helper's `pre-complete`, `post-immediate`, `post-plus-1`, and `post-plus-2` snapshot blocks from the auto-staged benchmark path before deciding whether the remaining regression is Tesla variant-sync work, EM rollout pressure, scenario drift, or helper overhead.

- `2026-04-18 | orbital-logistics | open | the cohort service path now skips stable signature rebuilds, reuses same-pass Need payload lookups, reuses the transponder registry across the same-pass retry, precomputes policy selector candidate order per build, keeps reconcile-time lease target counts local, stages a same-build ordered ready-job list, memoizes bound-silo lookups for each uplink across one service pass, threads that memo through launch, derives ready-job counts from the staged ready list, feeds coordinator GUI counts from the service snapshot, now lets coordinator output pages reuse those same staged job counts instead of rescanning the job map again, lets the open-panel refresh loop reuse the stored unit-number record instead of re-normalizing it twice, now reuses resolved cohort entities per unit number while refreshing multiple open panels, now seeds the shared open-panel context with coordinator pending-state so multiple coordinator panels on the same surface stop rereading the queue status during the same repaint pass, now only allocates the per-panel context when a repaint is actually due, now lazily reads coordinator pending-state in that open-panel pass so surfaces with only selector, transponder, or uplink panels stop paying for queue-status checks they will not use, now keeps that shared context scalar-only so entity-specific cached records do not bleed across different open terminals, reuses the current uplink record across the live summary/control refresh split, reuses that same uplink record while threshold drafts and dropdown drafts repaint the open panel, now trusts that same seeded uplink record through `refresh_player_gui`'s uplink summary branch instead of rereading the root-state fallback there too, reuses cached root/uplink state through click-driven uplink actions, now hoists that seeded uplink record once for the remaining release and oversize click branches instead of rereading the same fallback per action, now seeds the coordinator click-path handoff with pending-state too so immediate button repaints stop falling back to live queue reads, now seeds the same pending-state during full coordinator refreshes so both the summary block and the control refresher can trust the handoff instead of checking the live queue again, now normalizes that full-refresh panel context once so the summary path and the control refresher share the same stable root/cohort handoff, now threads the already-opened entity through more direct refresh callers so `refresh_player_gui` and `refresh_player_gui_controls` can trust that handoff sooner instead of rediscovering the same terminal on those local repaint paths, now trusts `refresh_player_gui`'s normalized panel-context table when reading coordinator pending-state instead of rechecking that invariant at the point of use, now trusts the already-seeded uplink record in `on_gui_click` instead of rebuilding the same fallback two lines later, now normalizes `refresh_player_gui_controls` the same way so its transponder, selector, coordinator, and uplink branches stop repeating the same optional-table dance before they reuse cached records, now seeds open-panel refreshes with the already-resolved entity plus transponder, selector, lease, and uplink records so the live GUI path stops rebuilding the same handoff one function later, now also carries the resolved bound silo through that same open-panel uplink handoff so live summary repaints stop repeating the silo lookup during the same refresh, now seeds the uplink record during full refresh handoff so both uplink GUI passes can trust the same cached record, now seeds the same kind of full-refresh record handoff for transponders and selectors so both their summary and control refreshers stop refetching current state during the same panel pass, now lets uplink output rendering reuse the already-resolved entity instead of normalizing the same record twice in one pass, now caches section roots through those click-driven panel actions instead of re-walking the same override, focus, binding, and threshold paths per branch, now mirrors that section-root reuse in the transponder and selector control refresher instead of walking the same fixed panel sections again, now lazily builds root-state and panel-context handoff in the draft-change handlers only after a real edit lands, reuses transponder and selector records plus coordinator pending-state through the same full-refresh handoff, shares the same-pass bound-silo cache with uplink output rendering, now lets the uplink dropdown refresh return the active values plus resolved selection instead of making the control pass reread widget tags immediately after rebuilding the list, now caches the preferred adjacent silo choice inside that same dropdown cache instead of re-evaluating ownership across the dropdown values on every repaint, now reuses that cached adjacent-silo choice when the uplink's `Use adjacent silo` button is clicked instead of immediately rescanning the nearby area again, now routes transponder, selector, and uplink draft typing back through the shared control-refresh path instead of maintaining three near-duplicate field refreshers, now reuses section roots inside the control refresher instead of tree-walking the same binding, selector, and coordinator paths again for release buttons and control rows, and drops the now-unused GUI path walker after the fixed-section panels moved to direct reads. The next likely orbital UPS hotspot is the remaining open-uplink binding-control work plus any leftover render-time lane passes around the dispatch surface.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/orbital-logistics.lua`
  - `Next safe move:` benchmark the remaining uplink loops plus `build_uplink_pages()` / GUI binding reads on the seeded orbital QC scenario, then decide whether the next safe win is a same-pass uplink-state staging shortcut or a narrow render-time binding cache that still preserves current lease and lane behavior.

- `2026-04-17 | orbital-logistics | open | selector lease cleanup, duplicate-target blocking, coordinator failover, demand-fairness rotation, and binding-loss recovery are covered, but the cohort helper still does not exercise a real launch-capable silo path.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/orbital-logistics.lua`, `.codex/skills/esir-dev/assets/zzz-orbital-logistics-qc_0.0.1/control.lua`
  - `Next safe move:` add one helper staging step or seeded save variant that brings a bound silo to `rocket_ready`, then assert `launch_count`, `last_launch_tick`, and post-launch lease behavior from the existing structured snapshots.

- `2026-04-17 | orbital-logistics-qc | open | the save-driven orbital cohort helper works during direct helper-aware runs, and the save catalog now records its helper requirement explicitly, but the wrapper-driven runtime lanes still rebuild mods-live without replaying zzz-orbital-logistics-qc automatically.`
  - `Files:` `.codex/skills/esir-dev/assets/zzz-orbital-logistics-qc_0.0.1/control.lua`, `.codex/skills/esir-dev/references/orbital-logistics-qc-helper.md`, `scripts/esir-dev-lib.ps1`
  - `Next safe move:` teach the lower QC sync path to honor save-catalog `helper_mods` during wrapper-driven runtime lanes so `qc-runtime -SaveId orbital-cohort` stops depending on manual or pre-staged helper sync.

- `2026-04-19 | orbital-logistics | open | click-driven uplink refreshes now seed the current bound silo the same way open-panel repaint refreshes do, so immediate post-click summaries stop repeating the same silo lookup when the binding did not change. The next likely orbital UPS hotspot is still the remaining open-uplink binding/control churn plus any leftover render-time lane passes around the dispatch surface.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/orbital-logistics.lua`
  - `Next safe move:` trim any remaining click-driven uplink refresh branches that still rebuild live binding state after the action has already resolved the selected silo or cached adjacent choice, then benchmark that narrower control path before touching broader render logic.

- `2026-04-19 | orbital-logistics | open | open-gui, silo-dropdown, and draft-text repaint callers now let the shared GUI refreshers seed their own panel context from the already-resolved entity instead of building one-use root/record handoff tables in the caller. The next likely orbital UPS hotspot is still the remaining open-uplink binding/control churn plus any leftover render-time lane passes around the dispatch surface.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/orbital-logistics.lua`
  - `Next safe move:` benchmark the remaining uplink binding/control repaint path, then decide whether the next safe win is a narrower adjacent-binding cache shortcut or a render-time lane staging trim that still preserves current lease and launch behavior.


- `2026-04-18 | flammable-ruptures | open | rupture job assembly now reuses one shared center-area candidate scan, local pipeline fallback rejects out-of-radius candidates before paying for `sqrt`, damage rings skip impossible distance math for victims outside the blast, dense pipe-network fire targeting now keeps a bounded nearest-target set instead of traversal-order cap wins, and the repo now ships a dedicated fluid-rupture QC helper plus rupture snapshot bridge. The save catalog now records the rupture helper explicitly and repo-side `runtime-benchmark` can restage it automatically; the remaining gap is still wrapper-driven runtime lanes.`
  - `Files:` `exotic-space-industries-remembrance/control.lua`, `exotic-space-industries-remembrance/scripts/control/fluid-rupture-effects.lua`, `exotic-space-industries-remembrance/scripts/control/flammable-fluids.lua`, `.codex/skills/esir-dev/assets/zzz-fluid-rupture-qc_0.0.1/control.lua`, `.codex/skills/esir-dev/references/fluid-rupture-qc-helper.md`, `.codex/esir/save-catalog.json`
  - `Next safe move:` teach the lower QC sync path to honor save-catalog `helper_mods` during wrapper-driven runtime lanes, then use the dedicated fluid-rupture save for `qc-runtime` smoke and any later dense-network rupture benchmark work without manual restaging.

- `2026-04-15 | runtime-scheduler | open | Vulcanus dormant ready queues still rely on false tombstones plus local active-surface bookkeeping.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/vulcanus-fumaroles.lua`
  - `Next safe move:` only migrate further after the shared scheduler can express the false-tombstone or equivalent live-set semantics without changing dormant-surface fairness behavior.

## Intentional Local

- `2026-04-19 | runtime-scheduler | intentional-local | matter stabilizer machine service still keeps its dense swap-remove per-surface queue local for fairness and UPS, while the module now uses `runtime-scheduler.lua` only for open-panel refresh buckets, counters, and runtime status.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/matter-stabilizer.lua`
  - `Next safe move:` leave the hot machine queue local unless the shared helper surface grows a dense-set scheduler abstraction; if GUI feedback still feels stale in playtests, trial a very slow open-only delayed refresh lane before considering any broader queue migration.

- `2026-04-15 | runtime-scheduler | intentional-local | steam train tracked and active unit sets are dense membership queues, not FIFO queues.`
  - `Files:` `exotic-space-industries-remembrance/scripts/control/steam-train.lua`
  - `Next safe move:` leave local unless there is a broader shared need for swap-remove queue helpers.
